 # Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files
.env
.env.*
!.env.example

# n8n workflow exports — kept locally for reference, not tracked (n8n cloud is source of truth).
# Outside public/ so builds/deploys can never ship them (they may embed API tokens).
n8n-exports/
public/n8n/

# NetSuite integration reference doc — kept locally, not tracked (documents internal
# NetSuite account ID, endpoints, and SuiteQL patterns; no secrets, just recon surface).
NetSuite_Integration_Reference.md

# Claude Code personal settings (session permission grants, may embed local secrets)
# Shared config (.claude/settings.json, agents/, skills/) stays committable.
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
